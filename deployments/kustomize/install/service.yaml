kind: Service
apiVersion: v1
metadata:
  name: mhx-ambulance-webapi
spec:
  selector:
    pod: mhx-ambulance-webapi-label
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: webapi-port
